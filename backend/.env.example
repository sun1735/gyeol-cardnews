# 서버 포트 (Railway 가 주입, 로컬은 4000)
PORT=4000

# Postgres 접속 문자열 (Railway Postgres 서비스에서 자동 주입)
#   예: postgresql://user:pass@host:5432/railway
DATABASE_URL=

# 선택 사항: 설정되면 gpt-4o-mini 로 카피 생성, 없으면 템플릿 폴백
OPENAI_API_KEY=

# 스토리지 드라이버 (현재 local 만 구현됨, s3 는 추후 추가)
STORAGE_DRIVER=local
